import { SafebrowsePipe } from './safebrowse.pipe';

describe('SafebrowsePipe', () => {
  it('create an instance', () => {
    const pipe = new SafebrowsePipe();
    expect(pipe).toBeTruthy();
  });
});
